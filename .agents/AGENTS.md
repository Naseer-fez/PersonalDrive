# Workspace Rules

## Testing Constraints
- We are currently in testing mode.
- For testing purposes, the frontend and API endpoints should assume/send the userid as `1`.
- Once all features are verified, this will be changed to dynamic user IDs.
